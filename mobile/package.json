{
  "name": "central-manutencao-se-mobile",
  "version": "1.9.3",
  "private": true,
  "description": "Camada Android nativa da Central de Manutenção SE.",
  "scripts": {
    "web:prepare": "node scripts/prepare-web.mjs",
    "android:add": "npm run web:prepare && npx cap add android",
    "android:configure": "node scripts/configure-android.mjs",
    "android:sync": "npm run web:prepare && npx cap sync android",
    "android:open": "npm run android:sync && npx cap open android",
    "android:doctor": "npx cap doctor"
  },
  "dependencies": {
    "@capacitor/action-sheet": "8.1.1",
    "@capacitor/android": "8.5.0",
    "@capacitor/app": "8.1.1",
    "@capacitor/camera": "8.2.2",
    "@capacitor/core": "8.5.0",
    "@capacitor/filesystem": "8.1.2",
    "@capacitor/share": "8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0",
    "esbuild": "0.28.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
